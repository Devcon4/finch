version: '3.4'

services:

# Custom services.

  chat:
    build: ./services/chatService
    environment:
      DB_HOST: .
      DB_PORT: 4261
      DB_USER: dev
      DB_DBNAME: chat
      DB_PASSWORD: FinchDev
    ports:
      - 4265:80
    networks:
      - dev_default

networks:
  dev_default:
    driver: overlay
    attachable: true